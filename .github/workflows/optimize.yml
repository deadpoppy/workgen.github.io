name: Optimize Assets
on: [push]
jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Optimize PNG
        run: |
          sudo apt-get install -y pngquant
          pngquant --quality=80-95 --force --ext .png public/images/logo.png 